package cityPlus;

public class Individualist extends Human {
	Individualist(int id, String typeId, Worldview worldview, Zone orig, Zone dest, double reduceCarTravel) {
		super(id, typeId, worldview, orig, dest, reduceCarTravel);
	}
}
