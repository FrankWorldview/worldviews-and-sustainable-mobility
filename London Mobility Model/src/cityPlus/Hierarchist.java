package cityPlus;

public class Hierarchist extends Human {
	Hierarchist(int id, String typeId, Worldview worldview, Zone orig, Zone dest, double reduceCarTravel) {
		super(id, typeId, worldview, orig, dest, reduceCarTravel);
	}
}
