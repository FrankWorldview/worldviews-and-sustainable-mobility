package cityPlus;

public class Egalitarian extends Human {
	Egalitarian(int id, String typeId, Worldview worldview, Zone orig, Zone dest, double reduceCarTravel) {
		super(id, typeId, worldview, orig, dest, reduceCarTravel);
	}
}
